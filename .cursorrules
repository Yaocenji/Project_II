# Project_II Unity 项目 Cursor 规则

## 项目概述
这是一个 Unity 2D 项目，使用 C# 编写，采用 Unity 新输入系统（Input System）。

## 命名空间规范

### 必须遵守的命名空间规则
- **所有 MonoBehaviour 脚本**：必须使用命名空间 `ProjectII`
- **角色相关脚本**：必须使用命名空间 `ProjectII.Character`
- **管理器脚本**：必须使用命名空间 `ProjectII.Manager`
- **输入系统相关**：使用命名空间 `Project_II.InputSystem`（注意下划线）

### 命名空间示例
```csharp
namespace ProjectII.Character
{
    public class CharacterController : MonoBehaviour { }
}

namespace ProjectII.Manager
{
    public class GameSceneManager : MonoBehaviour { }
}
```

## C# 代码规范

### 命名规范
遵循微软 C# 命名规范：https://learn.microsoft.com/en-us/dotnet/standard/design-guidelines/naming-guidelines

- **类名**：PascalCase（如 `CharacterController`）
- **方法名**：PascalCase（如 `UpdateDirection()`）
- **属性名**：PascalCase（如 `CurrentSpeedState`）
- **私有字段**：camelCase（如 `moveInput`）
- **常量**：PascalCase（如 `MaxSpeed`）
- **枚举**：PascalCase（如 `SpeedState`）
- **枚举值**：PascalCase（如 `SpeedState.Idle`）

### 代码组织

#### 字段声明顺序
1. `[Header]` 属性分组
2. `[SerializeField]` 私有字段（用于 Inspector 显示）
3. 公共属性（使用 PascalCase）
4. 私有字段（使用 camelCase）

#### 示例结构
```csharp
[Header("Input System")]
[SerializeField] private InputAction_0 inputActions;

[Header("Rigidbody")]
[SerializeField] private Rigidbody2D rb;

public Vector2 Direction { get; private set; }
private Vector2 moveInput;
```

### 文档注释
- **所有公共类、方法、属性**：必须使用 XML 文档注释（`/// <summary>`）
- **方法参数**：使用 `<param name="paramName">` 描述
- **返回值**：使用 `<returns>` 描述（如适用）

#### 示例
```csharp
/// <summary>
/// 根据InputAction的输入，更新运动方向
/// </summary>
public void UpdateDirection()
{
    // 实现
}

/// <summary>
/// 设置目标朝向方向（由Mouse脚本调用）
/// </summary>
/// <param name="direction">归一化的目标朝向方向</param>
public void SetTargetLookDirection(Vector2 direction)
{
    // 实现
}
```

### Unity 特定规范

#### MonoBehaviour 生命周期
- **Awake()**：初始化引用、注册到管理器
- **OnEnable()**：启用输入系统、订阅事件
- **OnDisable()**：禁用输入系统、取消订阅事件
- **Start()**：初始化需要其他对象已创建的逻辑
- **Update()**：读取输入、更新状态
- **FixedUpdate()**：物理相关操作（移动、旋转等）
- **OnDestroy()**：清理资源、注销注册

#### 执行顺序
- 使用 `[DefaultExecutionOrder(-100)]` 确保管理器优先执行
- GameSceneManager: `-100`
- InputManager: `-99`

#### 单例模式
使用属性单例模式，通过 `FindObjectOfType` 延迟查找：

```csharp
private static InputManager instance;

public static InputManager Instance
{
    get
    {
        if (instance == null)
        {
            instance = FindObjectOfType<InputManager>();
        }
        return instance;
    }
}
```

#### 输入系统
- **所有输入**：必须从 `InputManager.Instance.InputAction` 获取引用
- **不要单独创建** `InputAction_0` 实例
- **设备类型检测**：使用 `InputManager.Instance.CurrentDeviceType`

#### 管理器注册
- **角色控制器**：在 `Awake()` 中注册到 `GameSceneManager`
- **鼠标控制器**：在 `Awake()` 中注册到 `GameSceneManager`
- **注销**：在 `OnDestroy()` 中注销

### 代码质量

#### 空值检查
- 所有引用类型访问前必须进行空值检查
- 使用 `Debug.LogWarning` 或 `Debug.LogError` 记录问题

#### 性能考虑
- 避免在 `Update()` 中进行昂贵的操作
- 使用缓存减少重复计算
- 物理操作放在 `FixedUpdate()` 中

#### 注释规范
- **中文注释**：用于解释业务逻辑和设计意图
- **英文注释**：用于技术细节（可选）
- **TODO 注释**：标记待完成功能

## Git 提交规范

### 说明
你是一个资深的代码提交专家。
当用户要求你生成 Commit Message 时，你必须严格遵守以下规范：


### 提交格式
```
【关键词】一句话标题

描述（可选）
```

### 关键词词表

#### 功能相关
- 【新增】新增功能、脚本、系统
- 【功能】功能实现、功能完善
- 【优化】性能优化、代码优化、逻辑优化
- 【重构】代码重构、架构调整
- 【完善】功能完善、细节完善

#### 修复相关
- 【修复】修复bug、修复错误
- 【修复】修复崩溃、修复异常
- 【修复】修复逻辑错误

#### 文档相关
- 【文档】更新文档、完善文档
- 【注释】添加注释、完善注释

#### 配置相关
- 【配置】配置文件修改
- 【设置】项目设置调整
- 【资源】资源文件添加、修改

#### 系统相关
- 【系统】系统实现、系统集成
- 【架构】架构设计、架构调整
- 【管理】管理器实现、管理逻辑

#### 输入相关
- 【输入】输入系统实现
- 【控制】控制器实现
- 【交互】交互逻辑实现

#### 角色相关
- 【角色】角色系统实现
- 【移动】移动系统实现
- 【动画】动画系统实现

#### UI相关
- 【UI】UI界面实现
- 【界面】界面设计、界面调整

#### 其他
- 【调整】参数调整、数值调整
- 【清理】代码清理、资源清理
- 【测试】测试代码、测试功能
- 【临时】临时修改、临时方案

### 提交示例
```
【新增】创建了玩家角色控制脚本；【修复】修复了移动方向归一化问题
```

## 文件组织

### 目录结构
- `Assets/Character/` - 角色相关脚本
- `Assets/Manager/` - 管理器脚本
- `Assets/编程文档/` - 项目文档
- `Assets/Prefabs/` - 预制体
- `Assets/Scenes/` - 场景文件

### 文件命名
- **脚本文件**：与类名完全一致（如 `CharacterController.cs`）
- **场景文件**：使用描述性名称（如 `Scene0.unity`）
- **预制体**：使用中文或英文描述性名称

## 最佳实践

### 1. 依赖注入
- 通过管理器获取引用，而不是使用 `FindObjectOfType` 在运行时查找
- 在 `Awake()` 中获取引用，在 `Start()` 中使用

### 2. 输入处理
- 在 `Update()` 中读取输入
- 在 `FixedUpdate()` 中应用物理操作
- 使用事件回调处理切换类输入（如 `sneak` 的 Press 交互）

### 3. 平滑插值
- 使用 `SmoothDamp` 和 `SmoothDampAngle` 进行平滑过渡
- 旋转使用 `Mathf.SmoothDampAngle` 处理角度环绕

### 4. 状态管理
- 使用枚举定义状态（如 `SpeedState`）
- 提供公共属性访问当前状态
- 在统一的方法中更新状态

### 5. 错误处理
- 使用 `Debug.LogWarning` 记录非致命问题
- 使用 `Debug.LogError` 记录致命错误
- 提供有意义的错误消息

## 代码生成提示

当生成新代码时，请确保：
1. ✅ 使用正确的命名空间
2. ✅ 添加 XML 文档注释
3. ✅ 使用 `[Header]` 组织序列化字段
4. ✅ 实现适当的生命周期方法
5. ✅ 注册到相应的管理器
6. ✅ 在 `OnDestroy()` 中清理资源
7. ✅ 进行空值检查
8. ✅ 遵循命名规范

## 常见模式

### 从 InputManager 获取输入引用
```csharp
private void GetInputActionFromInputManager()
{
    if (inputActions == null)
    {
        ProjectII.Manager.InputManager inputManager = ProjectII.Manager.InputManager.Instance;
        if (inputManager != null && inputManager.InputAction != null)
        {
            inputActions = inputManager.InputAction;
        }
        else
        {
            Debug.LogError("无法从InputManager获取InputAction引用！请确保场景中存在InputManager。");
        }
    }
}
```

### 注册到 GameSceneManager
```csharp
private void RegisterToGameSceneManager()
{
    ProjectII.Manager.GameSceneManager gameSceneManager = ProjectII.Manager.GameSceneManager.Instance;
    if (gameSceneManager != null)
    {
        gameSceneManager.RegisterPlayerCharacter(this);
    }
    else
    {
        Debug.LogWarning("GameSceneManager 未找到，CharacterController 无法注册。");
    }
}
```

## 注意事项

1. **不要**在 Inspector 中手动赋值 `InputAction_0`，应该通过 `InputManager` 获取
2. **不要**在运行时使用 `FindObjectOfType` 频繁查找对象，应该通过管理器获取引用
3. **不要**忘记在 `OnDestroy()` 中注销注册和清理资源
4. **不要**在 `Update()` 中进行物理操作，应该使用 `FixedUpdate()`
5. **确保**所有公共 API 都有 XML 文档注释
