
# 脚本 ProjectII.Weapon.BulletBase
继承：MonoBehaviour

所有子弹的基类。
目前的Layer：
Player 玩家角色
Ally 友军（游戏暂未涉及，留作冗余）
Enemy 敌人
FriendlyFire 我方火力（我方发射的子弹应设为此Layer）
EnemyFire 敌方火力（敌方发射的子弹应设为此Layer）

# 成员 GameObject 发射者

# 公有成员 枚举/布尔 方式
该子弹可能是即时命中/物理实体

# 公有成员 LayerMask
选择哪些层是和子弹有所碰撞u

# 方法 Init
在实际使用中，在实例化后立即调用以保证在Start前调用该函数。
传入参数：
GameObject 发射者
即时命中/物理实体类型

根据发射者的Layer，设置自身的Layer
如果是物理实体的

# 方法 重写 Start
如果是即时命中类型的子弹，那么在这里进行射线检测，要用LayerMask。
如果击中，调用Hit
如果是物理实体类型的子弹，那么不干什么。
以右为射线方向。

# 方法 触发器触发
如果是物理实体类型的子弹，那么调用Hit。

# 纯绪方法 击中 Hit

